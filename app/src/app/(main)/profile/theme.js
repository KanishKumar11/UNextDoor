import React from 'react';
import ThemeSettingsScreen from '../../../features/profile/screens/ThemeSettingsScreen';

/**
 * Theme settings route
 * This route renders the ThemeSettingsScreen component
 */
export default function ThemeRoute() {
  return <ThemeSettingsScreen />;
}
